
@inline(a = "a")
function main1<T, K>(arg1: T, arg2: K) : Result<i32, Error> {
    let a = (Struct {
        a: 12 + 3,
        b: a.b(),
    },2);

    while let Enum::One(a) = (Struct<i32> {
        a: 3 + 3-4
    }) {

    }

    let b_func = a.func<i32>();
}
